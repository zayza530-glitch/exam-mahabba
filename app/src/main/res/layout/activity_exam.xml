<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/examRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary">

    <!-- Custom Exam Status Bar (Ringan 36dp) -->
    <LinearLayout
        android:id="@+id/customStatusBar"
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:background="@color/bg_card"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="14dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tvBatteryStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔋 100%"
            android:textColor="@color/accent_teal_light"
            android:textSize="12sp"
            android:textStyle="bold" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/tvExamTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Exam Mahabba • Terkunci"
            android:textColor="@color/text_secondary"
            android:textSize="12sp" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- Tombol Guru Buka Kunci -->
        <TextView
            android:id="@+id/btnUnlockTeacher"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔑 Buka"
            android:textColor="@color/text_muted"
            android:textSize="12sp"
            android:padding="6dp" />
    </LinearLayout>

    <!-- WebView Google Form Terproteksi Penuh -->
    <WebView
        android:id="@+id/webViewExam"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/customStatusBar"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Overlay Pelanggaran (Muncul jika siswa berbuat curang) -->
    <FrameLayout
        android:id="@+id/overlayWarning"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#F20F172A"
        android:visibility="gone"
        android:clickable="true"
        android:focusable="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="32dp"
            android:background="@drawable/bg_card_rounded"
            android:orientation="vertical"
            android:padding="26dp"
            android:gravity="center">

            <TextView
                android:id="@+id/tvWarnIcon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="⚠️"
                android:textSize="48sp" />

            <TextView
                android:id="@+id/tvWarnTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/cheat_warning_title"
                android:textColor="@color/danger_red"
                android:textSize="18sp"
                android:textStyle="bold"
                android:layout_marginTop="12dp" />

            <TextView
                android:id="@+id/tvWarnMessage"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/cheat_warning_msg"
                android:textColor="@color/text_secondary"
                android:textSize="13sp"
                android:gravity="center"
                android:layout_marginTop="8dp" />

            <TextView
                android:id="@+id/tvStrikeCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pelanggaran: 1 / 3"
                android:textColor="@color/warning_yellow"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginTop="12dp" />

            <Button
                android:id="@+id/btnContinueExam"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="20dp"
                android:background="@drawable/btn_primary_gradient"
                android:text="Lanjutkan Ujian"
                android:textAllCaps="false"
                android:textColor="@color/text_primary"
                android:textStyle="bold"
                app:backgroundTint="@null" />

            <Button
                android:id="@+id/btnUnlockEmergency"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/bg_input_field"
                android:text="Buka Kunci Pengawas (PIN)"
                android:textAllCaps="false"
                android:textColor="@color/text_secondary"
                android:textSize="12sp"
                app:backgroundTint="@null" />
        </LinearLayout>
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
